import React from "react";

const NoUserSelected = () => {
    return (
        <div className="flex-9 border-l-2 basis-full flex items-center justify-center">
            Please add or select a user to chat with
        </div>
    );
};

export default NoUserSelected;